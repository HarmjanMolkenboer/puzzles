import { PuzzlesizePipe } from './puzzlesize.pipe';

describe('PuzzlesizePipe', () => {
  it('create an instance', () => {
    const pipe = new PuzzlesizePipe();
    expect(pipe).toBeTruthy();
  });
});
