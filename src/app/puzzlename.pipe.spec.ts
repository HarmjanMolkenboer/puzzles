import { PuzzlenamePipe } from './puzzlename.pipe';

describe('PuzzlenamePipe', () => {
  it('create an instance', () => {
    const pipe = new PuzzlenamePipe();
    expect(pipe).toBeTruthy();
  });
});
